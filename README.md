# Keycloak Client Security Starter

Данный стартер:
- выполняет получение access токена keycloak перед каждым исходящим запросом (выполненном с помощью RestClient'а)
- сохраняет токен в кеше на заданное время
- подставляет токен в заголовок Authorization для исходящих запросов